{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php artisan migrate --force && php-fpm & nginx -g 'daemon off;'",
    "restartPolicyType": "ON_FAILURE"
  }
}
